{
  "name": "ghost-form",
  "version": "3.0.0",
  "description": "Privacy-first anti-phishing Chrome Extension with on-device ML",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "cross-env NODE_ENV=production vite build",
    "test": "jest --config jest.config.json"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.24.0",
    "babel-jest": "^29.7.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "jest-chrome": "^0.8.0",
    "vite": "^5.3.1"
  }
}
